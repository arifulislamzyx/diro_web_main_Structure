export { auth as middleware } from '@/authentication/auth';
// import authOptions  from '@/authentication/auth';
// import { NextAuthMiddleware } from 'next-auth/middleware';

// export const middleware = NextAuthMiddleware(authOptions);

// export const config = {
//   matcher: ['/protected-route/:path*', '/another-protected-route'], // Routes to protect
// };
